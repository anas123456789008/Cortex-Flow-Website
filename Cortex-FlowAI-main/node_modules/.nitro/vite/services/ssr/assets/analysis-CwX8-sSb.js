import { t as AppShell } from "./app-shell-C7qeheSU.js";
import { r as useTransactions, t as formatCurrency } from "./finance-CljcG_X8.js";
import { useMemo } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { Bar, BarChart, CartesianGrid, Line, LineChart, PolarAngleAxis, RadialBar, RadialBarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
//#region src/routes/_authenticated/analysis.tsx?tsr-split=component
function AnalysisPage() {
	const { data: txns = [] } = useTransactions();
	const stats = useMemo(() => analyze(txns), [txns]);
	return /* @__PURE__ */ jsx(AppShell, { children: /* @__PURE__ */ jsxs("div", {
		className: "animate-fade-up space-y-6",
		children: [
			/* @__PURE__ */ jsx("div", {
				className: "flex flex-wrap items-end justify-between gap-3",
				children: /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("h1", {
					className: "font-display text-3xl font-bold sm:text-4xl",
					children: "Financial Analysis"
				}), /* @__PURE__ */ jsx("p", {
					className: "mt-1 text-sm text-muted-foreground",
					children: "Deep dive into your spending patterns and trends."
				})] })
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "grid gap-4 lg:grid-cols-2",
				children: [
					/* @__PURE__ */ jsx(Card, {
						title: "Spending Trend",
						subtitle: "Daily expenses over 30 days",
						children: /* @__PURE__ */ jsx(ResponsiveContainer, {
							width: "100%",
							height: 240,
							children: /* @__PURE__ */ jsxs(LineChart, {
								data: stats.trend,
								children: [
									/* @__PURE__ */ jsx(CartesianGrid, {
										stroke: "rgba(255,255,255,0.06)",
										vertical: false
									}),
									/* @__PURE__ */ jsx(XAxis, {
										dataKey: "day",
										stroke: "#aab0d6",
										fontSize: 10,
										tickLine: false,
										axisLine: false
									}),
									/* @__PURE__ */ jsx(YAxis, {
										stroke: "#aab0d6",
										fontSize: 10,
										tickLine: false,
										axisLine: false
									}),
									/* @__PURE__ */ jsx(Tooltip, { content: /* @__PURE__ */ jsx(GlassTip, {}) }),
									/* @__PURE__ */ jsx(Line, {
										type: "monotone",
										dataKey: "amount",
										stroke: "#8b5cf6",
										strokeWidth: 2.5,
										dot: false
									})
								]
							})
						})
					}),
					/* @__PURE__ */ jsx(Card, {
						title: "Category Breakdown",
						subtitle: "This month",
						children: /* @__PURE__ */ jsx(ResponsiveContainer, {
							width: "100%",
							height: 240,
							children: /* @__PURE__ */ jsxs(BarChart, {
								data: stats.categories,
								layout: "vertical",
								children: [
									/* @__PURE__ */ jsx(CartesianGrid, {
										stroke: "rgba(255,255,255,0.06)",
										horizontal: false
									}),
									/* @__PURE__ */ jsx(XAxis, {
										type: "number",
										stroke: "#aab0d6",
										fontSize: 10,
										tickLine: false,
										axisLine: false
									}),
									/* @__PURE__ */ jsx(YAxis, {
										type: "category",
										dataKey: "name",
										stroke: "#aab0d6",
										fontSize: 11,
										width: 90,
										tickLine: false,
										axisLine: false
									}),
									/* @__PURE__ */ jsx(Tooltip, { content: /* @__PURE__ */ jsx(GlassTip, {}) }),
									/* @__PURE__ */ jsx(Bar, {
										dataKey: "value",
										fill: "#8b5cf6",
										radius: [
											0,
											4,
											4,
											0
										]
									})
								]
							})
						})
					}),
					/* @__PURE__ */ jsx(Card, {
						title: "Income vs Expense",
						subtitle: "Last 6 months",
						children: /* @__PURE__ */ jsx(ResponsiveContainer, {
							width: "100%",
							height: 240,
							children: /* @__PURE__ */ jsxs(BarChart, {
								data: stats.monthly,
								children: [
									/* @__PURE__ */ jsx(CartesianGrid, {
										stroke: "rgba(255,255,255,0.06)",
										vertical: false
									}),
									/* @__PURE__ */ jsx(XAxis, {
										dataKey: "month",
										stroke: "#aab0d6",
										fontSize: 10,
										tickLine: false,
										axisLine: false
									}),
									/* @__PURE__ */ jsx(YAxis, {
										stroke: "#aab0d6",
										fontSize: 10,
										tickLine: false,
										axisLine: false
									}),
									/* @__PURE__ */ jsx(Tooltip, { content: /* @__PURE__ */ jsx(GlassTip, {}) }),
									/* @__PURE__ */ jsx(Bar, {
										dataKey: "income",
										fill: "#3ddc97",
										radius: [
											4,
											4,
											0,
											0
										]
									}),
									/* @__PURE__ */ jsx(Bar, {
										dataKey: "expenses",
										fill: "#fb7185",
										radius: [
											4,
											4,
											0,
											0
										]
									})
								]
							})
						})
					}),
					/* @__PURE__ */ jsx(Card, {
						title: "Savings Rate",
						subtitle: "Goal: 20% of income",
						children: /* @__PURE__ */ jsxs("div", {
							className: "relative",
							children: [/* @__PURE__ */ jsx(ResponsiveContainer, {
								width: "100%",
								height: 240,
								children: /* @__PURE__ */ jsxs(RadialBarChart, {
									innerRadius: "65%",
									outerRadius: "100%",
									data: [{
										name: "rate",
										value: stats.savingsRate,
										fill: "#8b5cf6"
									}],
									startAngle: 210,
									endAngle: -30,
									children: [/* @__PURE__ */ jsx(PolarAngleAxis, {
										type: "number",
										domain: [0, 100],
										tick: false
									}), /* @__PURE__ */ jsx(RadialBar, {
										background: { fill: "rgba(255,255,255,0.06)" },
										dataKey: "value",
										cornerRadius: 20
									})]
								})
							}), /* @__PURE__ */ jsxs("div", {
								className: "pointer-events-none absolute inset-0 flex flex-col items-center justify-center",
								children: [/* @__PURE__ */ jsxs("div", {
									className: "font-mono text-4xl font-bold gradient-text",
									children: [stats.savingsRate.toFixed(0), "%"]
								}), /* @__PURE__ */ jsx("div", {
									className: "mt-1 text-xs text-muted-foreground",
									children: "of income saved"
								})]
							})]
						})
					})
				]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "rounded-2xl border border-violet/30 bg-gradient-to-br from-violet/15 via-indigo/10 to-transparent p-6 backdrop-blur-xl",
				children: [/* @__PURE__ */ jsx("h2", {
					className: "font-display text-lg font-semibold",
					children: "Analysis summary"
				}), /* @__PURE__ */ jsx("p", {
					className: "mt-2 text-sm leading-relaxed text-muted-foreground",
					children: stats.totalSpent === 0 ? "No data yet. Upload a bank statement or add a few transactions to generate a full analysis." : `You spent ${formatCurrency(stats.totalSpent)} across ${stats.txCount} transactions in the last 30 days. Your top spending category is ${stats.topCategory ?? "—"}, accounting for ${stats.topShare.toFixed(0)}% of expenses. Your current savings rate is ${stats.savingsRate.toFixed(0)}% — ${stats.savingsRate >= 20 ? "above the recommended benchmark." : "below the 20% benchmark planners typically recommend."}`
				})]
			})
		]
	}) });
}
function Card({ title, subtitle, children }) {
	return /* @__PURE__ */ jsxs("div", {
		className: "rounded-2xl border border-border bg-glass p-5 backdrop-blur-xl",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "mb-3",
			children: [/* @__PURE__ */ jsx("h3", {
				className: "font-display text-base font-semibold",
				children: title
			}), subtitle && /* @__PURE__ */ jsx("p", {
				className: "text-xs text-muted-foreground",
				children: subtitle
			})]
		}), children]
	});
}
function GlassTip({ active, payload, label }) {
	if (!active || !payload?.length) return null;
	return /* @__PURE__ */ jsxs("div", {
		className: "rounded-lg border border-border bg-popover/95 px-3 py-2 text-xs backdrop-blur-xl",
		children: [label && /* @__PURE__ */ jsx("div", {
			className: "mb-1 font-semibold",
			children: label
		}), payload.map((p, i) => /* @__PURE__ */ jsxs("div", {
			className: "flex items-center gap-2",
			children: [
				/* @__PURE__ */ jsx("span", {
					className: "h-2 w-2 rounded-full",
					style: { background: p.color || p.fill }
				}),
				/* @__PURE__ */ jsxs("span", {
					className: "capitalize text-muted-foreground",
					children: [p.name, ":"]
				}),
				/* @__PURE__ */ jsx("span", {
					className: "font-mono",
					children: formatCurrency(p.value)
				})
			]
		}, i))]
	});
}
function analyze(txns) {
	const now = /* @__PURE__ */ new Date();
	const thirty = /* @__PURE__ */ new Date();
	thirty.setDate(thirty.getDate() - 30);
	const recent = txns.filter((t) => new Date(t.occurred_on) >= thirty);
	const trendMap = /* @__PURE__ */ new Map();
	for (let i = 29; i >= 0; i--) {
		const d = /* @__PURE__ */ new Date();
		d.setDate(d.getDate() - i);
		trendMap.set(d.toISOString().slice(5, 10), 0);
	}
	let totalSpent = 0, totalIncome = 0;
	const catMap = /* @__PURE__ */ new Map();
	for (const t of recent) {
		const key = t.occurred_on.slice(5, 10);
		const amt = Number(t.amount);
		if (t.type === "expense") {
			totalSpent += amt;
			trendMap.set(key, (trendMap.get(key) ?? 0) + amt);
			catMap.set(t.category, (catMap.get(t.category) ?? 0) + amt);
		} else totalIncome += amt;
	}
	const categories = Array.from(catMap, ([name, value]) => ({
		name,
		value
	})).sort((a, b) => b.value - a.value).slice(0, 6);
	const topCategory = categories[0]?.name;
	const topShare = totalSpent ? (categories[0]?.value ?? 0) / totalSpent * 100 : 0;
	const savingsRate = totalIncome ? Math.max(0, (totalIncome - totalSpent) / totalIncome * 100) : 0;
	const trend = Array.from(trendMap, ([day, amount]) => ({
		day,
		amount
	}));
	const monthly = [];
	for (let i = 5; i >= 0; i--) {
		const d = new Date(now.getFullYear(), now.getMonth() - i, 1);
		let mi = 0, me = 0;
		for (const t of txns) {
			const td = new Date(t.occurred_on);
			if (td.getMonth() === d.getMonth() && td.getFullYear() === d.getFullYear()) if (t.type === "income") mi += Number(t.amount);
			else me += Number(t.amount);
		}
		monthly.push({
			month: d.toLocaleDateString("en", { month: "short" }),
			income: mi,
			expenses: me
		});
	}
	return {
		trend,
		categories,
		monthly,
		totalSpent,
		txCount: recent.length,
		topCategory,
		topShare,
		savingsRate
	};
}
//#endregion
export { AnalysisPage as component };
